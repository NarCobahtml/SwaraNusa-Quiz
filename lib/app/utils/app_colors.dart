import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Primary
  static const primary = Color(0xFF3E5C4A);

  // Background / Surfaces
  static const background = Color(0xFFF7F5F2);
  static const surface = Color(0xFFDAD7CD);
  static const secondary = Color(0xFFA3B18A);

  // Accent
  static const gold = Color(0xFFF59E0B);
  static const success = Color(0xFF588157);

  // Error / Warning
  static const error = Color(0xFFD32F2F);
  static const errorLight = Color(0xFFEF5350);

  // Text
  static const textDark = Color(0xFF3E5C4A);
  static const textLight = Color(0xFFF7F5F2);
  static const textMuted = Color(0xFF6B6B6B);

  // Utility
  static const primaryDark = Color(0xFF2A3D32);
  static const primaryLight = Color(0xFF4A6B57);
  static const divider = Color(0xFFC8C4BB);

  // Overlay (shadow/gradient)
  static const overlay = Color(0xFF000000);
  static const transparent = Colors.transparent;
}
